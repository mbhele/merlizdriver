<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Driver Details</title>
    <style>
        body {
            background-color: #000; /* Black background */
            color: #fff; /* White text */
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }

        h1, h2 {
            color: #ffd700; /* Gold text */
            text-align: center;
        }

        .details-container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #111; /* Darker background for content */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(255, 215, 0, 0.5); /* Gold shadow */
        }

        .profile-picture {
            display: block;
            margin: 0 auto;
            border-radius: 50%;
            width: 150px;
            height: 150px;
            object-fit: cover;
            border: 3px solid #ffd700; /* Gold border */
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ffd700; /* Gold border */
        }

        th {
            background-color: #222; /* Darker background for header */
            color: #ffd700; /* Gold text */
        }

        td {
            background-color: #111; /* Slightly darker black for table cells */
        }

        .button {
            display: inline-block;
            padding: 10px 20px;
            margin-top: 20px;
            background-color: #ffd700; /* Gold background */
            color: #000; /* Black text */
            text-decoration: none;
            border-radius: 5px;
            text-align: center;
            margin-bottom: 20px;
        }

        .button:hover {
            background-color: #ffbf00; /* Slightly darker gold on hover */
        }
    </style>
</head>
<body>
    <h1>Driver Details</h1>
    <div class="details-container">
        <% if (driver.profilePicture) { %>
            <img src="<%= driver.profilePicture %>" alt="Driver Profile Picture" class="profile-picture">
        <% } %>

        <h2><%= driver.name %></h2>
        <p><strong>Email:</strong> <%= driver.email %></p>
        <p><strong>Phone:</strong> <%= driver.phone %></p>

        <h2>Vehicle Information</h2>
        <table>
            <tr>
                <th>Make</th>
                <th>Model</th>
                <th>Plate Number</th>
            </tr>
            <tr>
                <td><%= driver.vehicle.make %></td>
                <td><%= driver.vehicle.model %></td>
                <td><%= driver.vehicle.plateNumber %></td>
            </tr>
        </table>

        <h2>Ride History</h2>
        <table>
            <tr>
                <th>Origin</th>
                <th>Destination</th>
                <th>Fare</th>
                <th>Status</th>
            </tr>
            <% driver.rideHistory.forEach(trip => { %>
            <tr>
                <td><%= trip.origin %></td>
                <td><%= trip.destination %></td>
                <td><%= trip.fare %></td>
                <td><%= trip.status %></td>
            </tr>
            <% }) %>
        </table>

        <a href="/drivers" class="button">Back to Driver List</a>
    </div>
</body>
</html>
