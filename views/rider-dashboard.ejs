<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rides</title>
    <link rel="stylesheet" href="/css/rides.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: black;
            color: white;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            box-sizing: border-box;
        }
        h1 {
            color: gold;
            text-align: center;
            margin-bottom: 30px;
            font-size: 2rem;
        }
        .container {
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
        .table-wrapper {
            width: 100%;
            max-width: 600px;
            background-color: #222;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            margin-top: 20px;
        }
        ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
        li {
            background-color: #333;
            margin: 10px 0;
            padding: 15px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 1rem;
        }
        li:hover {
            background-color: #555;
        }
        a {
            color: gold;
            text-decoration: none;
            flex-grow: 1;
            margin: 0 10px;
        }
        a:hover {
            text-decoration: underline;
        }
        .company-name {
            color: gold;
            text-align: center;
            margin-top: 10px;
            font-size: 1.5rem;
        }
        @media (max-width: 768px) {
            h1 {
                font-size: 1.5rem;
            }
            .table-wrapper {
                padding: 15px;
            }
            li {
                font-size: 0.9rem;
                padding: 12px;
            }
        }
        @media (max-width: 480px) {
            h1 {
                font-size: 1.2rem;
            }
            .table-wrapper {
                padding: 10px;
            }
            li {
                font-size: 0.8rem;
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Rides Booked by Customers</h1>
        <div class="company-name">Merliz</div>
        <div class="table-wrapper">
            <ul>
                <% riders.forEach((rider, index) => { %> <!-- Loop through riders instead of drivers -->
                    <li>
                        <span class="number"><%= index + 1 %>.</span>
                        <a href="/admin/rides/<%= rider._id %>"><%= rider.username %></a>
                    </li>
                <% }) %>
            </ul>
        </div>
    </div>
</body>
</html>
